import { IconProps } from "@/types/common.types";
import Icon from "@images/password.svg?react";

const PasswordIcon: React.FC<IconProps> = (props) => {
    return <Icon {...props} />;
};

export default PasswordIcon;
