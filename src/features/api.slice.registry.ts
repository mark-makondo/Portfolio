import type { SliceType } from "../app/app.types";

// initial api injectpoints
import "./projects/services/projects.api";

// rtk slices
const slices: Record<string, SliceType> = {};

export default slices;
